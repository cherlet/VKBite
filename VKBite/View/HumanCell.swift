import UIKit

class HumanCell: UICollectionViewCell {
    // MARK: Properties
    static let identifier = "HumanCell"
    
    // MARK: UI Elements
    
    // MARK: Setup
    func configure(with human: Human) {
    }
}
